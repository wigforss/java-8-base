name: Test CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Copy settings.xml
        run: cp $GITHUB_WORKSPACE/../.github/maven/settings.xml $HOME/settings.xml
      - name: View file
        run: cat $HOME/settings.xml

