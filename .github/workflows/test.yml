name: Test CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Copy settings.xml
        run: ls -l
      - name: View file
        run: ls -l $HOME

