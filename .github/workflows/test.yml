name: Test CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Copy settings.xml
        run: ls -l $GITHUB_WORKSPACE
      - name: View file
        run: ls -l $HOME

